# 上半身动捕：第二阶段（实验）

当前支持九套 Live2D 的身体侧倾，以及纸信圈儿“原子时报晓”310504 的小幅上臂、前臂跟随。尚未实现手掌、手指或全身动捕。
在开启动捕前勾选“上半身跟随（实验）”；运行期间选项锁定，需关闭动捕后再更改。
双肩完整入镜并正对摄像头，首次有效双肩姿态成为中立基准。校准按钮同时重置头部和身体基准。

## 已实现

- 复用面部动捕的同一条摄像头视频流，不额外请求摄像头或麦克风。
- MediaPipe Pose Landmarker Lite 在 CPU 本地运行，身体推理最高约 6.25 FPS，面部最高约 12.5 FPS；实际速度受设备性能影响。
- 用视频宽高修正后的双肩连线计算侧倾，独立于头部角度；控制输入限制为 ±12 度，以 0.45 增益作用于模型中立姿态。
- 仅绑定实际存在的 `ParamBodyAngleZ`，保留渲染器的平滑和模型自身最小/最大值钳制，不控制衣服、饰品或未确认的参数。
- 肩膀置信度不足、出画、严重侧身，或姿态鼻尖与当前人脸位置不匹配时拒绝身体样本。请保持只有一个人入镜，这些检查并非身份识别。
- 身体模型加载超时/失败或推理出错时回退面部动捕；迟到的加载结果会释放。关闭、切皮肤、隐藏和退出仍释放摄像头及两个识别器。
- 丢失人脸或双肩时不继续保持旧身体姿态，回到模型中立状态。

## 模型调查

使用仓库中的 Cubism Core 读取真实 `.moc3` 参数，并将身体侧倾参数增加 5.4 度验证网格响应。九套均有实际网格变化。
这证明参数可用，不等于已经用真人摄像头验证侧倾方向和观感。

| 角色/皮肤 ID | ParamBodyAngleZ 原始范围 | 第一阶段 |
| --- | --- | --- |
| 纳西索斯 315701 / 315702 | -10 至 10 | 身体侧倾 |
| 可燃点 307301 | -45 至 15 | 身体侧倾 |
| 可燃点 307304 | -30 至 30 | 身体侧倾 |
| 纸信圈儿 310502 / 310503 | -10 至 10 | 身体侧倾 |
| 纸信圈儿 310504 | -30 至 10 | 身体侧倾 |
| 37 306604 | -30 至 15 | 身体侧倾 |
| 37 306605 | -10 至 10 | 身体侧倾 |
| 夏利 301701 | Spine，不使用上述参数 | 暂不支持动捕 |

## 第二阶段：310504 手臂跟随

1. 选择纸信圈儿“原子时报晓”，先勾选“上半身跟随（实验）”，再勾选“手臂跟随（实验）”。默认均关闭，运行期间不能修改。
2. 开启摄像头，正对镜头，让肩、肘、腕尽量完整入镜，保持舒适姿势约半秒。每个通道需要连续三个稳定样本建立独立基准。
3. 轻缓抬臂或弯肘，观察小幅相对变化。使用“校准正面姿态”可重新建立全部基准；换到不支持的皮肤或取消上半身跟随会取消手臂选项。

同一 Pose 识别结果提供肩、肘、腕位置，不新增摄像头或模型推理。角度以双肩轴线为参照并修正视频宽高比，降低身体倾斜对抬臂估计的影响。
上臂输入相对基准限制为 ±45 度，前臂为 ±60 度，映射后的每个模型参数偏移最多 ±9，继续使用平滑和模型范围钳制。
腕部丢失仅移除该侧前臂通道；肘部丢失移除该侧上臂和前臂通道。双肩或人脸丢失则全部回到中立，不冻结旧动作。

实际 `.moc3` 参数与网格检查确认以下左右侧和方向；左右指角色的解剖学左右，非画面左右。

| 参数 | 实际范围 | 正向输入对应的映射 |
| --- | --- | --- |
| `ParamArmLxuanzhuan` | -25 至 60 | 左上臂抬起，增益 +0.2 |
| `ParamArmRxuanzhuan` | -40 至 10 | 右上臂抬起，增益 -0.2 |
| `ParamLSArmxuanzhuan` | -60 至 60 | 左前臂弯曲，增益 +0.15 |
| `ParamRSArmxuanzhuan` | -60 至 40 | 右前臂弯曲，增益 -0.15 |

只绑定以上四个参数且要求全部存在；不改动手掌、拉伸或变形参数。基准是用户启动时的姿态，模型基础仍是作者的待机姿态，因此不是 1:1 骨骼重定向。
目前只使用画面内二维角度；肢体朝向镜头时投影缩短，交叉或遮挡时也可能不准确。置信度、边界和长度检查只能拒绝部分异常情况，不能保证所有姿态可靠。
自动测试覆盖角度、左右独立、身体倾斜、遮挡、校准、限幅和资源释放；实际渲染器的合成输入预览用于检查小幅动作和恢复中立。尚未完成真人摄像头下的准确度、方向和性能验证。

其他模型可能含有匿名或作者自定义参数，不能据命名扫描断言它们完全没有手臂能力。

## 后续顺序

1. 在真实摄像头上校验身体与手臂方向、幅度、镜像直觉、遮挡回退及 CPU 占用。
2. 根据试用结果调整 310504 的阈值和增益，再逐个调查其他皮肤；不直接套用当前参数映射。
3. 肩臂稳定后才考虑手掌；不使用拉伸或变形参数冒充手指控制，不承诺所有皮肤支持同样动作。

## 新增模型与许可

文件：`web/body-pose.task`，5,777,746 字节。
来源：[Google MediaPipe Pose Landmarker Lite，float16 revision 1](https://storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task)。
下载文件 SHA-256：`59929e1d1ee95287735ddd833b19cf4ac46d29bc7afddbbf6753c459690d574a`。

继续使用已有的 MediaPipe Tasks Vision 0.10.21 运行库。运行库的 Apache-2.0 许可不能自动视作模型权重的再分发许可；模型专门授权尚未核实（NOASSERTION），分发前需确认。
原始资源归属和其他许可见 [THIRD_PARTY.md](THIRD_PARTY.md)。不上传、不录像、不使用麦克风。
